function App() {
  return (
    <main
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        background: '#fff',
        padding: 24,
      }}
    >
      <h1
        style={{
          color: '#dc2626',
          fontSize: 'clamp(44px, 8vw, 96px)',
          fontWeight: 900,
          lineHeight: 1,
          textAlign: 'center',
          letterSpacing: 0,
        }}
      >
        RESERVA FLOW AI PRODUCTION TEST
      </h1>
    </main>
  );
}

export default App;
